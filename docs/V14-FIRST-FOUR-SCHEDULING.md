# V14 first-four scheduling evidence

The observed first four native deliveries occurred atGF2188,2757,3378,3466 with68,88,70,124 points (350 total). A passing local mechanism does not establish higher overall throughput. The bounded source is `artifacts/v14-first-four-scheduling.json`, covering0–4000 with13 projection checks against full native JSON, exact job events and native chef/cannon/food transitions.

The clearest avoidable future-order dependency is Raspberry recipeIndex7 while no dish has yet been served. P0 spends1530–1627 adding its missing chopped flavor to a partial bowl already at14.283287 mixing seconds. Native insertion reduces that bowl's mixing progress; its later complete three-ingredient dough must mix again. At1872 P0 is free after plating Chocolate index3, but native heat arbitration selects this future Raspberry bowl3 at10.0999947 seconds, with10.9000053 seconds remaining before the21-second guard. P0's resulting work lasts1872–2128: native Mixed at1987, whole bowl pickup1995, exact basket5/fryer15 transfer2061 and empty bowl home14 return2128. This mechanism independently passes native identity/food/input checks; its timing priority is the issue.

At that1872 admission, first meal plate10 has been ready on lower-right47 since1768 and P2 is already natively loaded in cannon84 since1818. `DispatchNativeHeatSafety` runs before normal `Central`/`FireLoaded`, so the future bowl Work takes the otherwise idle central chef. P3 remains inside serial meal2 assembly1769–2274. The first cannon waits loaded1818–2071 (253 frames,4.2167 seconds). P3 reaches a valid completed temporary-place boundary at2032 and interrupts to fire2032–2072, retaining its original meal/source leases. Native flight starts2071, ends2122, first plate is picked up2147 and served2188. This bounded cannon interruption enables service earlier in the observed schedule; it does not cause the waiting interval.

There are zero cooperative-sauce events in this prefix. The two-sauce job uses its serial fallback, including nearby staging. Disabling cooperative sauces alone does not explain this case. A lookahead4 comparison is better grounded: index7 is outside the initial ordinary recipe window, removing this specific early Raspberry mixing/heat dependency while retaining the first Chocolate order. That is a proposed native configuration experiment, not a computed counterfactual timing gain. The entire first-four slowdown also contains pot parking/restore, ingredient supply, sauce routing and further cannon service dependencies; the256-frame Raspberry job alone must not be equated with total lost time.

No active Work, recipe, native processing clock or game state was changed for this diagnosis.
