# V12 native storage deadlock and bounded recovery

Frozen V12-b (`ab961385e7fc5a616fcf9b81d3ffd25a0cbddcc9a63b10106ee2edef4d12c418`) stopped at gameplay frame1564 with the explicit23-second pot guard, zero deliveries and zero score. The final cleanup frame is recorded separately. No native burnt-food observation is required to classify this controller deadline failure. The source trace is `artifacts/native-round-v12/trial001.jsonl.gz`; the file-only diagnostic is `artifacts/native-round-v12-heat-observer.jsonl`.

At892 the common scheduler correctly selected exact pot7 and harvested it into the first unplated hotdog. At895 it parked full ordinary dough6; native offmix stability completed at1110. At1104 cooked pot2 became the earliest unowned heat obligation. Chef0 had a feasible2.496-second approach/input estimate against7.650 seconds remaining to the guard. At1110 both centers became blocked, and all four workers were idle by1200. The clock continued normally until the guard failed.

The missing resource was an output slot. Counter32 held reserved raw sausage145;61 held reserved raw sausage153;33 held the first unplated hotdog141;37 held parked dough6. The four original plate counters still held four clean plates. Workspace42 was empty but intentionally protected for the head meal. Chopped bun151 already waited on board23, and pot2 contained exact native Cooked Frankfurter. Both normal future-hotdog output selection and whole-pot parking used the same exhausted ordinary counter pool. Blocking all unrelated work made that shortage persistent.

The next source adds one exception: an explicitly selected urgent original pot can use its own emptied chopped-bun board as the temporary unplated output. `CanStageExactPotHarvestOnSource` requires the actual mature original pot, exact chopped bun attached to a central chopping board, observed source/item identities, free source/item/pot resources and the existing complete direct-harvest route budget. Generic future production retains its ordinary storage restriction. This does not grant access to workspace42.

The resulting actions are ordinary `take(board23)`, `combine(pot2)`, `place(board23)` for the already addressed second meal. Its Work owns board23, bun151 and pot2. Because the queued final action still uses board23, staged pickup release keeps the board locked. The pot releases only after its combine action completes and telemetry shows the exact held hotdog plus the original empty pot with reset progress. A subsequent native pot loader may then run; finishing the old job cannot release that newer ownership. Final callback requires the same hotdog attached to the retained board and empty chef hands before recording the meal as staged.

`HeatRecoverySelfTest` passes33 assertions using unchanged GF1104/GF1200 snapshots and explicitly synthetic post-action observations. It covers the original storage shortage, exact-pot selection, generic future rejection, source reservation during pickup/combine, strict pot-release evidence, successor ownership, preserved active work and final placement failure. Negative cases reject leased boards, wrong buns, insufficient deadline budget, missing identity, wrong pot contents and occupied chefs. Existing common heat25, pot54, cannon34 and375 related assertions pass on the same isolated build. These are offline assertions; they do not establish that a new native trial completes the recovery or scores5000.
