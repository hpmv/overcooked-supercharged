`RegistryObserver-r1a` refreshes one registered entity's actual metadata at an explicit paused, input-fenced bridge boundary. It addresses the Story 1-1 raw fish registration arriving before native `ServerWorkableItem.StartSynchronising` has registered `GetNextPrefab()`. The initial registration has no spawn list; later component inspection of raw entity51 showed its real `SpawnableEntityCollection`.

The module is `framework-run/modules/RegistryObserver-r1a/RegistryObserver.r1a.dll`, SHA256 `eae67bfc264679d192681bc712a460f7fe01f2004d168cb63127b6e2d69b7a63`, entry type `SuperchargedPatch.Authoring.Modules.RegistryObserverModule`. It compiles against frozen core X. No activation or lifecycle hook is needed after loading it into slot `registry-observer`.

Call `hot-call` with operation `refresh` and args `{"entityId":51}` after pausing and fencing input. The module resolves exactly one current registered object, copies its actual name, position, component names, synchronization order and ordered spawn names, and appends one normal `EntityRegistryData` observation to the pending controller output. The caller must wait for the controller inspection to show the new metadata before using it, then release the fence normally. An absent native spawn collection remains absent; an observed empty collection is represented by its actual empty list.

The module checks object incarnation and both initial fixed-ID sets before publication. It does not call the whole-scene refresh, reset observation caches, change native state, commit a frame, or replace initial rigidbody/attachment membership. Running, unfenced, ambiguous/missing entities, invalid poses and changed identities are rejected before publication. The original first registration position remains preserved by the host.

The focused harness passed24 checks, including compiled module calls against the frozen-X build. Results: `artifacts/framework-registry-observer-r1a-tests.json`. Native metadata reception and subsequent preparation remain separate runtime evidence.

Revision r2 adds explicit `observe-absent` reconciliation for a later dynamic ID. DLL: `framework-run/modules/RegistryObserver-r2/RegistryObserver.r2.dll`; SHA256 `29cf483ba847b3d9aeb255255532abdd8cf7b3b452557122c58c442fb242e432`. It passed35 focused/compiled-call checks in `artifacts/framework-registry-observer-r2-tests.json`. Native r1a refresh exposed raw51's `ChoppedSushiFish` list; subsequent native chopping produced prepared53, but the old physical proxy52 lacked a controller retirement receipt.

`observe-absent` requires `entityId`, exact `sceneMetadataRefreshes` from module status, and a nonzero `priorBodyInstanceId` from the retained prior native receipt. It rejects initial objects, a present ID in either native lookup or registry list, or the prior body instance still registered under another ID. It rechecks the input fence, epoch, identities and initial sets before publishing the existing **framework-only** retirement message. It does not remove a native registry entry or destroy an object.

The returned proof explicitly labels `source=observed-native-registry-absence` and `historicalRemovalEventObserved=false`. The caller's prior identity comes from its separate recorded evidence; this operation proves current registry absence, not a past RemoveEntry callback or destruction of every inactive/unregistered Unity object. Preserve the bridge operation receipt alongside the host trace, because the existing retirement wire record alone carries only the entity ID.
