# User-requested pause

Work is paused. No background continuation is requested. All three agents were interrupted. The previous primary and lab game processes (65624 and71732) are absent and both loopback endpoints refuse connections. See `artifacts/user-pause-status.json`.

## Verified checkpoint

- Best completed adaptive authored run: V21, native Carnival3-4/4P/270 seconds, **2720 =2040 base+680 tips**,25 deliveries,0 deductions. It used an existing process kitchen restart, not a fresh process.
- Evidence: `docs/V21-COMPLETED-ROUND.md`, `artifacts/native-round-v21-native-score-audit.json`, `lab/artifacts/native-round-v21-final.png`.
- V21 exact input movie: `routes/probes/v21-completed-inputs.jsonl.gz`, SHA256 `0ec23f8e81f8f43479eba5b545c0a38804711063ee09fad3b1253be702a540b4`;16206 original requests/16203 one-frame steps. Playback remains untested.
- Previous completed best V16:2612/24. V19 and V20:2396/22 with identical verified native score ledgers. V17:2500/23.
- Only completed fresh full input replay remains the older authored V14 movie:2042/20, matching exact inputs/native gameplay events with separately reported raw physics/observation differences.
- **Zero qualifying5000-point fresh-start runs. Task is not complete.**

## Interrupted trials and pending candidate

- V22 full native attempt, `artifacts/native-round-v22`: last watched GF9603,1484/14,timer109.9283. Partial; no completed-round claim.
- V21 buffer1 full attempt, `artifacts/native-round-v21-buffer1`: last watched GF3276,356/4,timer215.384. Partial; no completed-round claim.
- Restart interrupted trials from native starts; do not restore their state or treat closed gzip as completion.
- Frozen V22 adds optional plate-first hotdog base production and per-action stationary transfers:2986 offline assertions passed. Its full trial enabled plate-first production; stationary remained off.
- Native stationary A/B passes: same21 actions/five jobs, endpoint1165→1123frames (0.700s),11 guarded input edges,2 fallback actions. Proof `artifacts/stationary-transfer-ab-proof.json`.
- Frozen V23 adds optional planner-wide `--stationary-target-transfers`, preserving per-action opt-out. DLL SHA256 `605df34a2dfa3a1a20b84646deddace75b64891396bf63204b0aea579bf8fa6e`; source tree `ae50c3d096b51616502eb0cb7f3358ab11a58721cc3125e8edb1533fc2fc4803`. Build passed unchanged source. Full fixture/core checks were launched; inspect their outputs and finish validation pinning before native use. Expected2441+48 fixture and545 core assertions; do not assume completion from launch.

## Highest-value findings for resumption

1. V22's first plate-first job holds original pot7/home19 after native consumption893 until final placement1013. Native pot stays empty/reset; P2 is idle/controlled/empty in UL and starts refill immediately on release. This establishes a120-frame blocked refill opportunity, not an end-to-end score gain. Evidence `artifacts/v22-plated-base-prefix-a.json.gz` and companion snapshots. Add exact staged release only after validating original action completion, empty/reset pot and same held plate; preserve subsequent ownership.
2. Service batching is weak: earlier V16 made17 trips for24 deliveries, with30.85 chef-seconds returning and14.13 boarding, plus flight/wait overhead. A competitive macro schedule is still missing. The proposed fixed8-second preboard wait and second stock refill had no positive witness under their existing guards; no policies were added. See `docs/PANTRY-BATCH-WAIT-REVIEW.md`.
3. Far-bowl direct Flour throws A/B failed; full near kit remained intact and source eventually landed on board59. B separately observes deflection/flight termination before board attachment; exact colliding object is unobserved. Three lower-diagonal candidates fail actual swept-box clearance, not an exhaustive impossibility proof. No far-bowl production feature was added. See `docs/FAR-BOWL-THROW-MECHANISM.md`.
4. V21 plated-onion/FIFO lifecycle audit was in progress with local_game_hooks; inspect completed files before redoing work. TAS agent was preparing commands for a single fresh V21 diagnostic exact replay using existing tools, with no native calls authorized to agents.

Keep the original Steam installation untouched. Existing game/plugin hashes and runtime isolation remain as documented. Do not run Build.ps1 over an active game. Original V17 and V20 gzip duplicates were losslessly archived and removed after complete decoded-byte verification; `.jsonl.xz`, manifests and removal receipts retain exact raw traces. V16/V19/V21 gzip sources remain available.
