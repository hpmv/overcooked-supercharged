# Story 1-1: end-to-end search

The active milestone is main-map 1-1 with four local chefs, using the isolated installed game and the user's framework. Carnival work and evidence remain preserved for later reuse.

**Session paused on 2026-09-08:** [fresh-session handoff](FRESH-SESSION-HANDOFF.md) contains current process/build identities, evidence, and next commands. Host v12d is a checked but unreleased build; 77 Story checks and the 69,399-exchange integration prefix pass. The running v12c host/game were left paused at frame1 with no pending actions; see `artifacts/story11/handoff-pause.json`.

The intended loop is native level load, observed kitchen/recipe mapping, a complete legal delivery, bounded action search with physics-tolerant rewind, and fresh-start input validation of selected routes. Native preparation, timing, scoring, ingredient ownership and accepted interactions remain authoritative. Physics tolerances apply to explicitly listed continuous fields and retain raw differences; they do not forgive missing objects, wrong food, or score changes.

The user explicitly requested removing the free preparation period. LevelSession r2 changes the in-memory Story 1-1 configuration's `m_recipesBeforeTimerStarts` from one to zero before gameplay, retaining its native round duration. Both native campaign Begin paths verify this scoped policy. No asset or save file is modified. This is an explicitly modified timer category, not an unmodified-game score.

Native loads confirm scene `s_sushi_1_1`, configuration `Sushi_1_1S_4P`, four server/client local users, and a 150-second round. At the first observed kitchen boundary, native elapsed time is 0.0333333351 seconds, `timerSuppressed=false`, and deliveries/score are zero. The complete second load took 14.4 seconds without restarting the game process (a later load with rendering capped at 60 FPS took 30.3 seconds). Evidence: `artifacts/framework-migration/story11-discovery-b/load.json`; timer mutation receipts: `framework-run/artifacts/level-session-transitions.jsonl`.

**Immediate timer verified:** `artifacts/story11/timer-120-neutral-b.json` records exactly 120 neutral advancing frames, native elapsed delta 1.9999986649 seconds, unchanged 150-second duration, unsuppressed timer, zero deliveries/score, and no rewind. Screenshot: `framework-run/artifacts/timer-120-neutral-b.png`. Reproduce on a fresh mapped load with `python scripts/verify_story11_timer.py --out <new-evidence-file>`. The first verifier attempt rejected the framework's empty four-chef graph header; the corrected verifier uses the existing empty-action validator. Both attempts are preserved.

The live registry contains 50 initial entities, including four chefs, four boards, four initial clean plates, fish/prawn crates, service and clean plate return. Native read-only component inspection confirms crate prefabs and clean return behavior. Navigation uses source geometry checked against live anchors; registered station trigger dimensions do not establish the entire solid collision mesh.

Work in progress:

- Complete: external level-session helper and immediate timer policy, with native load receipts and 43 offline selector/signature checks.
- In progress: mapped Story 1-1 controller setup and first-dish action execution; the working discovery host retains actual native registry and state deltas.
- Complete for native startup: ScriptedRound r2 wraps the actual native six-entry scripted prefix and weighted suffix, with 62 focused offline checks. A native reload reports the correct two opening Fish orders, cursor two, unchanged weighted frequencies `[0,0]`, and native generator `ScriptedRoundData.GetNextRecipe`. Post-warmup kitchen checkpoints are captured; loading frame zero remains explicitly ambiguous and ineligible.
- Native rewind restoration now succeeds after correcting the four corner-counter annotations in host v12b. `artifacts/framework-migration/story11-idle-parity-b` records verified native restore attempt 44 at frame 61: the baseline's complete observed entity, body, food, order and clock state matches exactly. The subsequent 60-frame idle continuation differs in chef height (0 versus approximately 0.05), exceeding the explicit physical bounds. Baseline frame 61 was already at Y=0; the original continuation rises toward 0.05 while replay stays at zero. Full parity is not established. The paired private-field diagnostic is `scripts/framework_story11_chef_correction_probe.py`.
- Native preparation/plating/delivery observations and short-horizon automated search.
- Explicit physics tolerance comparisons and fresh-start replay validation.

Native action progress:

- `artifacts/framework-migration/story11-first-delivery-b`: chef 45 fetched fish from crate 30 and placed raw entity 51 on board 31, while chef 44 collected plate 2. All three native transfer predicates completed in 59 advancing frames. The original raw-food predicate failed because native raw fish has null assembled composition; its exact prefab, spawn path and ServerWorkableItem state now identify it instead.
- `artifacts/framework-migration/story11-first-delivery-c`: native chopping produced entity 53, `ChoppedSushiFish`, with assembled ingredient `SushiFish` UID 23600. No cooking duration/progress was modified. The runner stopped at a missing retirement observation for the raw fish's former physics proxy 52.
- RegistryObserver r1a successfully refreshed raw 51's actual ordered spawn metadata (`ChoppedSushiFish`) without touching the native world or initial capture sets. RegistryObserver r2 then proved proxy 52 was absent from the current native registry and published a framework-only absence reconciliation. This is explicitly not a historical native removal event. Host v12c fixes the old unmapped-proxy lookup failure. Its integration check replays the unchanged 69,399-exchange recorded prefix, followed by an explicitly synthetic codec suffix built from the actual absence receipt; native execution of the complete delivery remains pending.
- `scripts/framework_story11_fresh_search.py` now runs four preparation candidates from ordinary fresh native loads, then replays the selected recorded inputs from a fifth load. This is a bounded fetch-to-board and parallel plate-pickup search, not a complete route optimizer. It retains raw differences and checks a named preparation-goal projection separately from full-state parity.
- The first fresh search attempt, `artifacts/framework-migration/story11-fresh-prep-a`, stopped before gameplay on early chef-registration height validation. Native registration Y differed by about 0.000151, while XZ and the corresponding settled pose matched. A phase-specific audit is being added; station 3D checks and the existing tolerance stay strict. This is distinct from the approximately 0.05 continuation difference in the idle rewind probe.
- The first-delivery runner now reconciles observed dynamic food/proxy metadata automatically while fenced, checks that publication leaves native food, physics and clocks unchanged, and moves the empty chopper away before the plate carrier approaches. These changes passed 29 offline checks; native validation is pending.

No Story 1-1 delivery, automated search improvement or positive score has yet been demonstrated. Source, failures and screenshots are retained. The timer milestone is independently verified and is not conditional on the remaining automation work.
