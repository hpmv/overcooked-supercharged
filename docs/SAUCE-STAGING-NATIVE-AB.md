# Native serial sauce parking experiment

The independently checked pair used 668 native frames (11.1333 seconds) with original temporary counter37 and 360 frames (6 seconds) with counter40: an observed reduction of308 frames, or5.1333 seconds. Both began atGF1433 and produced the same original plate13 with complete native recipe125780 on output49. This is a local mechanism/timing experiment, not a high-score or full-planner throughput proof.

`artifacts/sauce-staging-ab-proof.json` is the final independent file-only report. It checks all six closed traces against the immutable1350-request prefix and identical87-request captured setup, including original one-frame step boundaries and native float input normalization. The two nine-action branch plans differ only in temporary place/take station37 versus40. Native mustard addition occurred atGF1609 in both arms; native ketchup occurred at2017 versus1709. Each addition requires the original held plate, a fresh ordinary pickup edge, native placement target72, the corresponding dispenser selection, and the frozen native food-tree recipe validator. The middle switch requires empty hands, native use target79 and a fresh use edge. Final source151 consumption, empty temporary counter, exact output plate, zero score/deliveries/deductions and absence of ruined food are verified.

Food/control projections match exactly at all1350 prefix and87 setup samples. Relevant raw starting state is **not exactly identical**: initial chef XZ differences range from0.0000077 to0.00002697 metres, with physical rotation/velocity and absolute clock differences retained in the report. No numeric tolerance hides those differences. Native timer, physics phase/count and food processing clocks match at branch start. The observed timing delta therefore carries an explicit raw-state-drift qualification; it is not presented as a completely identical-world controlled measurement. The first native mustard timing and composition nevertheless match, and the branches diverge in the deliberately changed subsequent parking loop.

Common setup uses only P3 inputs:18 neutral settling frames, park original bowl6 on33, take original bowl3 from mixer14, then2 neutral frames. The corrected waits were caught before execution; `artifacts/sauce-staging-wait-correction.json` preserves old/new hashes and the old preparation receipt. Bowl3 remains Unmixed at10.9458485; bowl6 remains Mixed at17.5832386. Their exact native food trees and mixing progress remain unchanged throughout each entire branch. The setup leaves both pots more than15 native seconds before burning; every subsequent sample remains below24 cooking seconds. Other chefs remain neutral, with P3 continuing to hold bowl3.

The checker uses frozen V13 assembly`60db3656d074464ec35d2ea5126ee0abff9073a148d188a7509cce107bb7a025` and verifies all71 frozen source-file hashes. Executing controller identity is operator-attributed: trace headers expose controller version, not an independent process-DLL receipt. Native instrumentation in every sample reports plugin`7d5fa9fb67a17cfc974d80e2416620bfa4f6ab2cc213ac8a33b890aa7b6fafbd`. Neutral connection-release observer events are retained and allowed; scoring/delivery events are rejected. File/config/plan/movie/result/checker hashes are included in the final report.

Run from the workspace root, choosing a new output path because existing reports are not overwritten:

```
dotnet run --project scripts/SauceABProbeCheck/SauceABProbeCheck.csproj -c Release -- routes/probes/sauce-staging-ab-check.json NEW_REPORT.json
dotnet run --project scripts/SauceABProbeCheck/SauceABProbeCheck.csproj -c Release -- selftest NEW_TEST_REPORT.json
C:\Python312\python.exe scripts/test_sauce_staging_preparation.py
```

`artifacts/sauce-staging-ab-check-tests01.json` records37 focused assertions using the captured setup and both native sauce transitions. Mutations reject missing input edges, wrong native targets/selection, replaced plate identity, incorrect food preparation, offmixer drift, unsafe heat budget, altered request boundaries and even tiny unrecorded same-run boundary movement. The11 preparation checks additionally verify effective18/2 waits, immutable prefix hashes, exactly two permitted branch changes and no-overwrite behavior. Tests mutate only in-memory evidence; they are not additional native runs.
